// The entry point. It creates the Vue app from your <App> component and mounts
// it into the #app element in index.html.
import { createApp } from "vue";
import App from "./App.vue";

createApp(App).mount("#app");
